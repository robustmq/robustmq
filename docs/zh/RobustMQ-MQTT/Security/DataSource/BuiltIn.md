# 内置数据源（Meta Service）

内置数据源就是将认证、授权、黑名单数据直接存储在 RobustMQ 的 Meta Service 中。

## 怎么用

- 无需额外配置，默认即可使用；
- 通过管理接口维护用户、ACL、黑名单；
- 数据变更后缓存实时更新，无同步延时；
- CONNECT 鉴权优先比对内存缓存。

## 注意事项

- 运维简单，适合快速落地；
- 如果已有统一账号系统，可切换到外部数据源（MySQL/Redis/HTTP 等）；
- 建议配合备份策略保障元数据可恢复。
